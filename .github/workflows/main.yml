name: CI

on: [push]

jobs:
  build:
    name: ESlLint Checks
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v1

      - name: "ESLint"
        uses: gimenete/eslint-action@1.0
